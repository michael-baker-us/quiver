name: Create a post
method: POST
url: "{{baseUrl}}/posts"
body:
  type: json
  content:
    title: Hello from quiver
    body: Git-friendly API collections
    userId: 1
    test: false
tests:
  - status: 201
  - jsonpath: $.title
    equals: Hello from quiver
