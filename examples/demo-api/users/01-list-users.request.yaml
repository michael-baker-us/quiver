name: List users
method: GET
url: https://petstore3.swagger.io/api/v3/pet/findByStatus?status=available
tests:
  - status: 200
  - header: content-type
    contains: application/json
  - jsonpath: $[0].id
    exists: true
capture:
  firstUserId: $[0].id
